# Phase 01.1: Xero Business Balance Integration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-04
**Phase:** 01.1-xero-business-balance-integration
**Areas discussed:** Token management, Account matching, Balance fetching scope, Code organization

---

## Token Management

| Option | Description | Selected |
|--------|-------------|----------|
| Share token file | Reuse .xero_token.json from yumyums Xero app. CLI reads and refreshes same token. | |
| Standalone CLI auth | Build a local HTTP callback server for OAuth2 flow | |
| Manual token entry | User pastes refresh token into .env | |
| Other (copy token functions) | Copy token persistence functions from yumyums app.py directly | ✓ |

**User's choice:** Copy the token persistence functions (load_token, save_token, refresh_token) directly from `~/projects/yumyums/accounting/xero/app.py` into the new module.
**Notes:** User provided the exact code to copy. CLI manages its own token file with the same pattern.

---

## Account Matching

| Option | Description | Selected |
|--------|-------------|----------|
| Replace mercury_id with xero_account_id | Add xero_account_id field to business accounts. Explicit mapping by Xero UUID. | |
| Match by account name | Match by name automatically. No new field but fragile. | |
| Keep both fields | Add xero_account_id alongside mercury_id. | |
| Other (xero_account_id + name matching) | Add field but use name-based matching like Monarch | ✓ |

**User's choice:** Add `xero_account_id` field to business accounts in payments.yaml, but use name-based matching similar to how Monarch matching works (via `monarch_match` pattern).
**Notes:** Follows the established Monarch pattern — explicit field with name-based resolution strategy.

---

## Balance Fetching Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Bank account balances only | Just BANK type accounts — simplest | |
| Bank + credit card balances | Include credit cards from Xero too | ✓ |
| Full financial picture | Banks, credit cards, and invoices/bills | |

**User's choice:** Bank + credit card balances
**Notes:** Aligns with how Monarch already provides both account types.

---

## Code Organization

| Option | Description | Selected |
|--------|-------------|----------|
| New xero_balances.py module | Standalone module like monarch_balances.py | ✓ |
| Add to coverage_report.py | Put alongside existing Mercury/Monarch functions | |
| Symlink/import from yumyums | Import from yumyums/accounting/xero/ directly | |

**User's choice:** New `xero_balances.py` module
**Notes:** Follows existing pattern (monarch_balances.py for Monarch, xero_balances.py for Xero).

## Claude's Discretion

- SDK vs raw HTTP for balance fetching (SDK preferred if straightforward)
- Cache TTL for Xero balance data

## Deferred Ideas

None
