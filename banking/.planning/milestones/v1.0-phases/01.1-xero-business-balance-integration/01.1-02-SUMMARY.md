---
phase: 01.1-xero-business-balance-integration
plan: 02
subsystem: xero-wiring
tags: [xero, mercury-removal, balance-resolution]
dependency_graph:
  requires: [xero_balances_module, xero_account_config]
  provides: [mercury_free_codebase, xero_balance_resolution]
  affects: [coverage_report.py, zoho_calendar_payments.py]
tech_stack:
  added: []
  patterns: [try_except_import_guard, graceful_degradation]
key_files:
  created: []
  modified: [coverage_report.py, zoho_calendar_payments.py]
decisions:
  - "Xero import uses try/except guard matching existing MonarchMoney pattern"
  - "resolve_balance checks xero_account_id before falling back to Monarch"
metrics:
  duration: "2 minutes"
  completed: "pending-checkpoint"
  tasks: 1
  files: 2
status: checkpoint-paused
---

# Phase 01.1 Plan 02: Wire Xero into Coverage Report + Zoho Calendar Summary

Replaced all Mercury balance references with Xero in coverage_report.py and zoho_calendar_payments.py, completing the Mercury-to-Xero migration for business account balances.

## What Was Built

### Task 1: Replace Mercury with Xero in coverage_report.py and zoho_calendar_payments.py

**coverage_report.py changes:**
- Added `from xero_balances import fetch_xero_balances` with try/except guard (matching MonarchMoney pattern)
- Removed `fetch_mercury_balances()` function entirely (27 lines)
- Updated `resolve_balance()` signature: `mercury_balances` -> `xero_balances`, checks `xero_account_id` instead of `mercury_id`
- Updated `print_report()` signature and internal call to use `xero_balances`
- Updated `main()` to call `fetch_xero_balances()` with graceful fallback (`if fetch_xero_balances else {}`)
- Updated "unknown balance" message from "Monarch/Mercury" to "Xero/Monarch"
- Updated module docstring

**zoho_calendar_payments.py changes:**
- Removed `fetch_mercury_balances` from coverage_report import
- Added standalone `from xero_balances import fetch_xero_balances` with try/except guard
- No other mercury references existed in the file (it passed empty `{}` to resolve_balance which is still valid)

### Task 2: Verify Xero integration runs without crashes (CHECKPOINT - PENDING)

Awaiting human verification that both scripts run without crashing.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | 7e39a1f | feat(01.1-02): replace Mercury with Xero in coverage_report.py and zoho_calendar_payments.py |

## Deviations from Plan

None -- plan executed exactly as written.

## Verification Results (Task 1)

- coverage_report.py: zero Mercury references (case-insensitive grep confirms)
- coverage_report.py: contains `fetch_xero_balances` import
- coverage_report.py: contains `xero_account_id` in resolve_balance
- coverage_report.py: correct resolve_balance signature
- coverage_report.py: parses as valid Python (ast.parse)
- zoho_calendar_payments.py: no `fetch_mercury_balances` reference
- zoho_calendar_payments.py: contains `fetch_xero_balances` import
- zoho_calendar_payments.py: parses as valid Python (ast.parse)

## Known Stubs

None -- all wiring is complete. The `xero_account_id: null` values in payments.yaml (from Plan 01) are the only remaining stubs, tracked in that plan's summary.

## Self-Check: PASSED

- [x] coverage_report.py modified with Xero integration
- [x] zoho_calendar_payments.py modified with Xero import
- [x] Commit 7e39a1f exists in git log
- [x] Zero Mercury references in modified files
