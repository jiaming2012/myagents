---
phase: 01.1-xero-business-balance-integration
plan: 03
type: execute
wave: 1
depends_on: ["01.1-02"]
files_modified:
  - zoho_calendar_payments.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [FCST-04]
must_haves:
  truths:
    - "zoho_calendar_payments.py calls fetch_xero_balances() before resolve_balance loop"
    - "resolve_balance() receives actual Xero balance data instead of hardcoded {}"
    - "FCST-04 description in REQUIREMENTS.md says Xero, not Mercury"
  artifacts:
    - path: "zoho_calendar_payments.py"
      provides: "Xero balance data flows through to resolve_balance"
      contains: "xero_balances = fetch_xero_balances()"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement text reflecting Xero migration"
      contains: "Xero for business accounts"
  key_links:
    - from: "zoho_calendar_payments.py:resolve_variable_amounts"
      to: "xero_balances.py:fetch_xero_balances"
      via: "guarded function call before resolve_balance loop"
      pattern: "fetch_xero_balances.*if.*fetch_xero_balances.*else"
---

<objective>
Close two verification gaps from Phase 01.1: (1) wire fetch_xero_balances() call into zoho_calendar_payments.py so Xero balance data actually flows through to resolve_balance(), and (2) update FCST-04 requirement text from "Mercury" to "Xero".

Purpose: Complete the Mercury-to-Xero migration that Plan 02 left partially wired in zoho_calendar_payments.py.
Output: Both files patched, zero verification gaps remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-xero-business-balance-integration/01.1-02-SUMMARY.md

<interfaces>
<!-- From coverage_report.py lines 260-261 -- the EXACT pattern to replicate -->
```python
# In main(), before the report:
xero_balances = fetch_xero_balances() if fetch_xero_balances else {}
```

<!-- From coverage_report.py line 124 -- resolve_balance signature -->
```python
def resolve_balance(account, monarch_balances, xero_balances):
```

<!-- From zoho_calendar_payments.py line 55-57 -- import already exists -->
```python
try:
    from xero_balances import fetch_xero_balances
except ImportError:
    fetch_xero_balances = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire fetch_xero_balances() call into zoho_calendar_payments.py</name>
  <files>zoho_calendar_payments.py</files>
  <read_first>
    - zoho_calendar_payments.py (full file -- focus on lines 268-299 resolve_variable_amounts function and lines 54-57 import)
    - coverage_report.py (lines 259-261 for the pattern to replicate)
  </read_first>
  <action>
In zoho_calendar_payments.py, modify the `resolve_variable_amounts` async function (starts at line 268).

Current code at line 294:
```python
balance, _ = resolve_balance(acct_config, monarch_balances, {})
```

Change the function to fetch Xero balances (guarded) AFTER fetching Monarch balances and BEFORE the loop. Insert one line after line 275 (`monarch_balances = await fetch_monarch_balances()`):

```python
xero_balances = fetch_xero_balances() if fetch_xero_balances else {}
```

Then change line 294 from:
```python
balance, _ = resolve_balance(acct_config, monarch_balances, {})
```
to:
```python
balance, _ = resolve_balance(acct_config, monarch_balances, xero_balances)
```

This matches the exact pattern used in coverage_report.py lines 260-261 (per D-10). The `fetch_xero_balances` import with try/except guard already exists at line 54-57, so the `if fetch_xero_balances else {}` guard works correctly when xero-python is not installed.

Do NOT change any other lines. Do NOT modify the function signature. Do NOT add logging or print statements.
  </action>
  <verify>
    <automated>python -c "
import ast, sys
tree = ast.parse(open('zoho_calendar_payments.py').read())
src = open('zoho_calendar_payments.py').read()
# Check 1: fetch_xero_balances() is called (not just imported)
assert 'fetch_xero_balances()' in src, 'fetch_xero_balances() never called'
# Check 2: resolve_balance no longer receives hardcoded {}
assert 'resolve_balance(acct_config, monarch_balances, {})' not in src, 'Still passing hardcoded {} to resolve_balance'
# Check 3: xero_balances variable is passed to resolve_balance
assert 'resolve_balance(acct_config, monarch_balances, xero_balances)' in src, 'xero_balances not passed to resolve_balance'
# Check 4: valid Python
print('All checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -n "fetch_xero_balances()" zoho_calendar_payments.py returns at least one line inside resolve_variable_amounts (not just the import)
    - grep "resolve_balance(acct_config, monarch_balances, {})" zoho_calendar_payments.py returns NO output
    - grep "resolve_balance(acct_config, monarch_balances, xero_balances)" zoho_calendar_payments.py returns exactly one match
    - python -c "import ast; ast.parse(open('zoho_calendar_payments.py').read())" exits 0
  </acceptance_criteria>
  <done>fetch_xero_balances() is called with guard in resolve_variable_amounts, and the result is passed as the third argument to resolve_balance() instead of hardcoded {}</done>
</task>

<task type="auto">
  <name>Task 2: Update FCST-04 requirement text from Mercury to Xero</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 21 specifically)
  </read_first>
  <action>
In .planning/REQUIREMENTS.md, line 21, change:

FROM:
```
- [ ] **FCST-04**: Forecast pulls real-time balances from Monarch Money for personal accounts and Mercury for business accounts (separate sources, not fallback)
```

TO:
```
- [ ] **FCST-04**: Forecast pulls real-time balances from Monarch Money for personal accounts and Xero for business accounts (separate sources, not fallback)
```

Only change "Mercury" to "Xero" on that one line. Do NOT modify any other lines in the file.
  </action>
  <verify>
    <automated>python -c "
lines = open('.planning/REQUIREMENTS.md').readlines()
fcst04 = [l for l in lines if 'FCST-04' in l][0]
assert 'Xero' in fcst04, f'FCST-04 does not mention Xero: {fcst04.strip()}'
assert 'Mercury' not in fcst04, f'FCST-04 still mentions Mercury: {fcst04.strip()}'
print('FCST-04 text updated correctly')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "FCST-04" .planning/REQUIREMENTS.md contains "Xero for business accounts"
    - grep "FCST-04" .planning/REQUIREMENTS.md does NOT contain "Mercury"
    - No other lines in the file are changed
  </acceptance_criteria>
  <done>FCST-04 requirement description accurately reflects that Xero (not Mercury) is the business account balance source</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan wires an existing guarded import into an existing function call.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-gap-01 | I (Information Disclosure) | fetch_xero_balances in zoho_calendar_payments.py | accept | fetch_xero_balances already has its own guards for missing credentials/tokens; no new credential handling added here |
</threat_model>

<verification>
After both tasks complete, re-run the verification checks from 01.1-VERIFICATION.md:

1. `python -c "import ast; ast.parse(open('zoho_calendar_payments.py').read())"` -- valid Python
2. `grep -n "fetch_xero_balances()" zoho_calendar_payments.py` -- called (not just imported)
3. `grep "resolve_balance(acct_config, monarch_balances, {})" zoho_calendar_payments.py` -- no hardcoded empty dict
4. `grep "FCST-04.*Mercury" .planning/REQUIREMENTS.md` -- no Mercury in FCST-04
5. `grep "FCST-04.*Xero" .planning/REQUIREMENTS.md` -- Xero present in FCST-04
</verification>

<success_criteria>
- zoho_calendar_payments.py data-flow trace shows FLOWING (not DISCONNECTED) for xero_balances
- REQUIREMENTS.md FCST-04 text matches actual system behavior (Xero, not Mercury)
- All Python files still parse as valid Python
- Phase 01.1 verification score improves from 9/11 to 10/11 (human verification still needed for runtime test)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-xero-business-balance-integration/01.1-03-SUMMARY.md`
</output>
